// import { Vault } from '../data/models';

// exports.vaultService = async (query, page, limit) => {

//     try {
//         var users = await Vault.find(query);
//         return users;
//     } catch (e) {
//         // Log Errors
//         throw Error('Error while Paginating Vaults');
//     }
// }